(function(){"use strict";function initGallery(){const mainImage=document.getElementById("pdpRefMainImage");const thumbs=Array.from(document.querySelectorAll(".pdp-ref-thumb"));const thumbRail=document.getElementById("pdpRefThumbs");if(mainImage&&thumbs.length){thumbs.forEach((thumb)=>{thumb.addEventListener("click",()=>{thumbs.forEach((item)=>item.classList.toggle("is-active",item===thumb));if(thumb.dataset.image){mainImage.src=thumb.dataset.image;mainImage.alt=thumb.getAttribute("aria-label")||mainImage.alt}})})}document.querySelectorAll("[data-thumb-direction]").forEach((button)=>{button.addEventListener("click",()=>{if(!thumbRail)return;const direction=Number(button.dataset.thumbDirection)||1;thumbRail.scrollBy({left:direction*thumbRail.clientWidth*.72,behavior:"smooth"})})});const zoomButton=document.querySelector(".pdp-ref-zoom");if(zoomButton&&mainImage){const modal=document.createElement("div");modal.className="pdp-ref-zoom-modal";modal.hidden=true;modal.innerHTML='<button type="button" aria-label="Tutup gambar">×</button><img alt="" />';document.body.appendChild(modal);const modalImage=modal.querySelector("img");const closeButton=modal.querySelector("button");function closeModal(){modal.hidden=true;document.body.style.overflow=""}zoomButton.addEventListener("click",()=>{modalImage.src=mainImage.src;modalImage.alt=mainImage.alt;modal.hidden=false;document.body.style.overflow="hidden";closeButton.focus()});closeButton.addEventListener("click",closeModal);modal.addEventListener("click",(event)=>{if(event.target===modal)closeModal()});document.addEventListener("keydown",(event)=>{if(event.key==="Escape"&&!modal.hidden)closeModal()})}}function initProductChoices(){const colorLabel=document.getElementById("selectedColor");document.querySelectorAll(".pdp-ref-swatch").forEach((button)=>{button.addEventListener("click",()=>{document.querySelectorAll(".pdp-ref-swatch").forEach((item)=>{item.classList.toggle("is-active",item===button);item.setAttribute("aria-pressed",item===button?"true":"false")});if(colorLabel)colorLabel.textContent=button.dataset.color||"Selected"})});const qtyInput=document.getElementById("pdpRefQty");document.querySelectorAll("[data-ref-qty]").forEach((button)=>{button.addEventListener("click",()=>{if(!qtyInput)return;const current=Number(qtyInput.value)||1;const delta=Number(button.dataset.refQty)||0;qtyInput.value=Math.min(10,Math.max(1,current+delta))})});const addButton=document.querySelector("[data-ref-add-cart]");addButton?.addEventListener("click",()=>{const quantity=Number(qtyInput?.value)||1;const switchType=document.getElementById("switchType")?.value||"Linear Red";const color=colorLabel?.textContent||"Stealth Black";const name=addButton.dataset.name||"Vortex VX Pro Mechanical";const price=Number(addButton.dataset.price)||1850000;if(window.NexCart){window.NexCart.add({name,price,qty:quantity,variant:`${color} / ${switchType}`})}else if(window.showNexToast){window.showNexToast(`${name} ditambahkan ke keranjang`)}const original=addButton.textContent;addButton.textContent="Berhasil Ditambahkan";addButton.classList.add("is-success");window.setTimeout(()=>{addButton.textContent=original;addButton.classList.remove("is-success")},1100)})}function initReviewTools(){const filter=document.getElementById("pdpReviewFilter");const sort=document.getElementById("pdpReviewSort");const list=document.getElementById("pdpReviewList");if(!list)return;const cards=Array.from(list.querySelectorAll(".pdp-ref-review"));function render(){const rating=filter?.value||"all";const sorted=[...cards].sort((a,b)=>sort?.value==="highest"?Number(b.dataset.rating)-Number(a.dataset.rating):Number(b.dataset.date)-Number(a.dataset.date));sorted.forEach((card)=>{card.hidden=rating!=="all"&&card.dataset.rating!==rating;list.appendChild(card)})}filter?.addEventListener("change",render);sort?.addEventListener("change",render);render()}function initAccordions(){const items=Array.from(document.querySelectorAll(".pdp-ref-accordion details"));items.forEach((item)=>{item.addEventListener("toggle",()=>{if(!item.open)return;items.forEach((other)=>{if(other!==item)other.open=false})})})}function initRelatedActions(){document.querySelectorAll(".pdp-ref-related .catalog-action-btn--wishlist").forEach((button)=>{button.addEventListener("click",()=>button.classList.toggle("is-active"))})}function init(){if(!document.body.classList.contains("page-product-detail"))return;initGallery();initProductChoices();initReviewTools();initAccordions();initRelatedActions()}if(document.readyState==="loading"){document.addEventListener("DOMContentLoaded",init,{once:true})}else{init()}})();